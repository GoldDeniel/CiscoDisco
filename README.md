This short script indicates the volume in the terminal, and sends packets to a specific ip. 

Usage (example):

$ python CiscoDisco.py \<ip\> \<port\>

If you don't give the args, a loopback address will be used.
